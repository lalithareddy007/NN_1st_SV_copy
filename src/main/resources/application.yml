server:
  #heroku
  port: $PORT
  #local
  #port: 8080
  servlet:
    context-path: /lms
spring:
  datasource:
    #for heroku
    url: jdbc:postgresql://ec2-3-217-251-77.compute-1.amazonaws.com:5432/d73fal507rokm8
    username: cyhhwycxvhgool  #changed as per Local DB
    password: 32283ade1303728d8066f6b5155f6b9900072e130996662720bbcf021a49cbf2   #changed as per Local DB
    #for local system
    #url: jdbc:postgresql://localhost:5432/LMS_DB
    #username: postgres  #changed as per Local DB
    #password: admin   #changed as per Local DB
  jackson.deserialization.fail-on-unknown-properties: true
  jackson.serialization.fail-on-empty-beans: false  # LMSPhase2 changes
  mvc.hiddenmethod.filter.enabled: true
  main.allow-bean-definition-overriding: true
  #hibernate ddl Auto for CURDOperations
  jpa.hibernate.ddl-auto: update


#File related Configurations
  
  Spring :
  http :
   multipart :
     enabled = true
               
  servlet:
    multipart:
      max-file-size: ${MAX_FILE_SIZE:8MB}
      max-request-size: ${MAX_REQUEST_SIZE:8MB}
      
  project:
  image: images/
      

logging:
  file:
    name: lmsservice.log
    path: ./
layout:
  pageInfo:
    program:
      idField: "programId"
      title: "Programs"
      searchField: "programName"
      fieldNames:
        programId: "Program Id"
        programName: "Program Name"
        programDescription: "Program Description"
        programStatus: "Program Status"
    programbatch:   
      idField: "batchId"
      title: "Batches"
      searchField: "batchName"
      fieldNames:
        batchId: "Batch Id"
        batchName: "Batch Name"
        batchDescription: "Batch Description"
        batchStatus: "Batch Status"
        batchProgramId: "Program Id"
        batchNoOfClasses: "No.of.classes"
    class:
      idField: "csId"
      title: "Classes"
      searchField: "classTopic"
      fieldNames:
        csId: "Class Id"
        batchId: "Batch Id"
        classNo: "Class No"
        classDate: "Class Date"
        classTopic: "Class Topic"
        classStaffId: "Staff Id"
        classDescription: "Description"
        classComments: "Comments"
        classNotes: "Notes"
        classRecordingPath: "Recording"
    attendance:
      idField: "attId"
      title: "Attendance"
      searchField: "classId"
      fieldNames:
        attId: "Attendance Id"
        classId: "Class Id"
        studentId: "Student Id"
        attendance: "Attendance"

